# python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# rust
beecore/target/
kernel/target/

# swift
BeehiveMac/.build/
# packaged app + iconset renders (rebuild: BeehiveMac/scripts/make_app.sh / make_icon.swift)
BeehiveMac/dist/

# test/bench scratch outputs
beecore/interop/.tmp/
beecore/bench/.tmp/

# exp5 listening pairs + token artifacts (hundreds of MB, regenerable:
# experiments/exp5_neural_base.py). exp3's small per-track subdirs stay tracked.
experiments/recon/*.wav
experiments/recon/*.dac

# local-session things, deliberately out of the project
.quarantine/
.claude/
.DS_Store

# third-party reference material (behavior references only -- never committed;
# see sonofaig-engineering skill: no third-party code/assets in the repo)
*.pdf
