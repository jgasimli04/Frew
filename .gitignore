# python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# rust
beecore/target/
kernel/target/
bearing-helix-research/target/

# swift
BeehiveMac/.build/
# packaged app + iconset renders (rebuild: BeehiveMac/scripts/make_app.sh / make_icon.swift)
BeehiveMac/dist/

# test/bench scratch outputs
beecore/interop/.tmp/
beecore/bench/.tmp/

# experiment outputs derived from commercial source tracks (third-party audio
# — never committed; regenerable from your own library via the exp scripts)
experiments/recon/

# stray local drafts
/import sys.py
/mg.py

# local-session things, deliberately out of the project
.quarantine/
.claude/
.DS_Store

# third-party reference material (behavior references only -- never committed;
# see sonofaig-engineering skill: no third-party code/assets in the repo)
*.pdf
